<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.sch.smktelkom_mlg.project.xiirpl109192939.voteapp" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" />
        <!-- <activity android:name=".RiwayatVote" /> -->
        <!-- <activity android:name=".HasilVote" /> -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".AddVote" />
        <activity
            android:name=".HomeVote"
            android:theme="@style/AppTheme.NoActionBar" />
        <!-- <activity android:name=".ProfileActivity"></activity> -->
        <activity android:name=".KetVoteActivity" />
        <activity android:name=".AddCandidates" />
    </application>

</manifest>